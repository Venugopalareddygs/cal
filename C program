#include <stdio.h>

int main() {
    int num1, num2, sub;

    printf("Enter the two number: ");
    scanf("%d %d", &num1,&num2);

    sub = num1 - num2;
    printf("sub= %d\n", sub);

    return 0;
}

